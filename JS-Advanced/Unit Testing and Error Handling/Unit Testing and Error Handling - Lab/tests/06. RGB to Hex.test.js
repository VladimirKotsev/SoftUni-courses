const { expect } = require(`chai`);

const { rgbToHexColor } = require(`../06. Check for Symmetry`);

describe(`Tests for function rgbToHexColor`, () => {

    it (`should return undefined for incorect red`, () => {

        
    })
});